import{_ as e,c as t,o,a5 as r}from"./chunks/framework.ClTy78B2.js";const m=JSON.parse('{"title":"Docker","description":"","frontmatter":{},"headers":[],"relativePath":"docs/deploy/docker.md","filePath":"docs/deploy/docker.md"}'),a={name:"docs/deploy/docker.md"},i=r('<h1 id="docker" tabindex="-1">Docker <a class="header-anchor" href="#docker" aria-label="Permalink to &quot;Docker&quot;">​</a></h1><p>This project makes use of <a href="https://www.docker.com" target="_blank" rel="noreferrer">Docker</a> to create an &quot;easy&quot; way of running the whole solution. However the resulting <a href="https://www.docker.com" target="_blank" rel="noreferrer">Docker</a> image of this solution will not be uploaded to a public <a href="https://hub.docker.com" target="_blank" rel="noreferrer">Docker Hub</a>. Any images that claim to be of this project are fraudulent or forks of this project. If we decide to publish an <em>official</em> <a href="https://www.docker.com" target="_blank" rel="noreferrer">Docker</a> image, this part of the documentation will be updated.</p><p><em>While the front-end is a <s>simple</s> <a href="https://www.solidjs.com" target="_blank" rel="noreferrer">SolidJS</a> app, which we could host on GithubPages, it will be served up by KoaJS in the docker image</em></p><h2 id="why" tabindex="-1">Why? <a class="header-anchor" href="#why" aria-label="Permalink to &quot;Why?&quot;">​</a></h2><p>Docker makes it easy to run applications without having anything installed on the host machine (other than docker).</p><h2 id="build" tabindex="-1">Build <a class="header-anchor" href="#build" aria-label="Permalink to &quot;Build&quot;">​</a></h2><p>Ensure that you have <a href="https://www.docker.com" target="_blank" rel="noreferrer">Docker</a> installed. Also check that you have created a <code>.env</code> file (based on the <code>.env.template file</code>) and that you have filled in the variables.</p><p>From the root directory of this project (where the <code>Dockerfile</code> is), run <code>sh ./scripts/buildDocker.sh</code>. This will get some info required from the <code>.env</code> that you should have created and start the process of building the image using <a href="https://www.docker.com" target="_blank" rel="noreferrer">Docker</a>.</p><h2 id="configure" tabindex="-1">Configure <a class="header-anchor" href="#configure" aria-label="Permalink to &quot;Configure&quot;">​</a></h2><p>If you want to tag and push this image to your own registry, you can simply change the properties that start with <code>DOCKER_</code> in the <code>.env</code>.</p><div class="important custom-block github-alert"><p class="custom-block-title">IMPORTANT</p><p>Do not try to impersonate this project by pushing this image to a container image library like <a href="https://hub.docker.com" target="_blank" rel="noreferrer">docker hub</a> with the same name or similar.</p></div>',11),c=[i];function h(s,n,l,d,p,u){return o(),t("div",null,c)}const k=e(a,[["render",h]]);export{m as __pageData,k as default};
